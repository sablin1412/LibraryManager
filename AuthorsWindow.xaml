<Window x:Class="LibraryManager.AuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="450" Width="550"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="AuthorsDataGrid" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="AuthorsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="100"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=d}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Margin="0,15,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Имя:" FontWeight="Bold"/>
                    <TextBox x:Name="FirstNameTextBox" Margin="0,5,0,10" Padding="2"/>
                    
                    <TextBlock Text="Фамилия:" FontWeight="Bold"/>
                    <TextBox x:Name="LastNameTextBox" Margin="0,5,0,10" Padding="2"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Страна:" FontWeight="Bold"/>
                    <TextBox x:Name="CountryTextBox" Margin="0,5,0,10" Padding="2"/>
                    
                    <TextBlock Text="Дата рождения:" FontWeight="Bold"/>
                    <DatePicker x:Name="BirthDatePicker" Margin="0,5,0,10"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Click="AddAuthor_Click" Margin="0,0,10,0" Padding="15,5" Background="#E6F4EA"/>
            <Button Content="Сохранить изменения" Click="EditAuthor_Click" Margin="0,0,10,0" Padding="15,5"/>
            <Button Content="Удалить" Click="DeleteAuthor_Click" Padding="15,5" Background="#FCE8E6"/>
        </StackPanel>
    </Grid>
</Window>